# Solution_RepositoryPattern

La solution est composé de trois couches (API + EFCore + Core (domain))
La solution est implementé avec le pattern Repository
Core comprend notre domain avec deux class (Author et Book)
